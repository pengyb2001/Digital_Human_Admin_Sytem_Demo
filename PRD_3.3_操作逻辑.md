# 3.3 操作逻辑

## 3.3.1 录入人物

### 3.3.1.1 入口位置
- **位置**：表格右上角，与批量操作按钮同一行
- **按钮样式**：主按钮（Primary），尺寸 Large，带“+”图标
- **按钮文字**：录入人物

### 3.3.1.2 录入方式
系统支持两种录入方式：

#### 方式一：新建Face ID
- **适用场景**：创建全新人物
- **操作流程**：
  1. 点击“录入人物”按钮
  2. 在弹窗中选择“新建Face ID”
  3. 输入新的Face ID（系统自动校验唯一性）
  4. 填写其他必填字段
  5. 点击“确定”完成录入

#### 方式二：关联已有人物
- **适用场景**：为已存在的人物添加新的形象
- **操作流程**：
  1. 点击“录入人物”按钮
  2. 在弹窗中选择“关联已有人物”
  3. 从下拉列表中选择已存在的Face ID
  4. 填写其他必填字段
  5. 点击“确定”完成录入
  6. 系统自动将该人物信息关联到选中的Face ID

### 3.3.1.3 必填字段
| 字段名 | 字段说明 | 校验规则 |
|--------|---------|---------|
| Face ID | 人物唯一标识 | 必填；新建时需校验唯一性，不能与已存在的Face ID重复 |
| 主播名称（对内） | 内部管理使用的名称 | 必填；仅内部可见 |
| 对外人物名 | 前端客户可见的名称 | 必填 |
| 类型 | 数字人来源类型 | 必填；选项：自研视频数字人、自研图片数字人、腾讯云数字人 |
| 性别 | 人物性别 | 必填；选项：男、女 |
| 年龄 | 人物年龄 | 必填；选项：青年、中年、老年 |

### 3.3.1.4 选填字段
| 字段名 | 字段说明 | 说明 |
|--------|---------|------|
| 头像 | 人物头像图片 | 支持上传，用于列表展示 |
| 授权开始时间 | 授权生效开始日期 | 日期选择器，格式：YYYY-MM-DD |
| 授权结束时间 | 授权失效结束日期 | 日期选择器，格式：YYYY-MM-DD |

### 3.3.1.5 业务规则
1. **Face ID唯一性校验**：
   - 新建Face ID时，系统实时校验是否已存在
   - 如已存在，提示“该Face ID已存在”，不允许提交
   - 关联已有人物时，从现有列表中选择，无需校验

2. **库区归属**：
   - 录入时自动归属到当前选中的Tab（公有/私有）
   - 私有库需填写“所属客户”字段

3. **默认值设置**：
   - 类型默认值：自研视频数字人
   - 性别默认值：男
   - 年龄默认值：青年

4. **提交后处理**：
   - 成功提交后，自动关闭弹窗
   - 显示成功提示：“人物添加成功”
   - 列表自动刷新，新人物显示在列表末尾
   - 表单自动重置

### 3.3.1.6 异常处理
- **校验失败**：表单字段校验不通过时，在对应字段下方显示错误提示，不允许提交
- **Face ID重复**：新建Face ID时如与已存在ID重复，显示错误提示
- **网络异常**：提交失败时显示错误提示，保留表单数据

---

## 3.3.2 批量操作

### 3.3.2.1 功能概述
批量操作功能用于对多个形象进行批量上架或批量下架操作，提高运营效率。

### 3.3.2.2 入口位置
- **位置**：表格上方，搜索筛选区下方
- **按钮样式**：默认按钮（Default），尺寸默认
- **按钮文字**：批量上架、批量下架

### 3.3.2.3 选择机制

#### 3.3.2.3.1 全选功能
- **位置**：形象表格表头第一列（选择框列）
- **功能**：点击表头复选框可全选/取消全选当前页所有形象
- **状态显示**：
  - 全部选中：复选框为选中状态
  - 部分选中：复选框为半选状态（indeterminate）
  - 未选中：复选框为未选中状态

#### 3.3.2.3.2 人物级选择
- **位置**：人物行第一列（选择框列）
- **功能**：点击人物行的复选框，自动选中该人物下的所有形象
- **状态显示**：
  - 该人物下所有形象都被选中：复选框为选中状态
  - 该人物下部分形象被选中：复选框为半选状态
  - 该人物下无形象被选中：复选框为未选中状态
- **交互逻辑**：
  - 选中人物：自动展开该人物，并选中其下所有形象
  - 取消选中人物：自动取消该人物下所有形象的选择

#### 3.3.2.3.3 形象级选择
- **位置**：形象行第一列（选择框列）
- **功能**：单独选择或取消选择某个形象
- **交互逻辑**：
  - 点击形象行的复选框，仅影响该形象的选择状态
  - 不影响其他形象或人物的选择状态

### 3.3.2.4 选择状态显示
- **位置**：批量操作按钮右侧
- **显示规则**：
  - 无选中时：不显示
  - 有选中时：显示“已选择 X 个形象”（X为选中数量）
  - 文字颜色：灰色（#999）

### 3.3.2.5 批量上架

#### 3.3.2.5.1 触发条件
- 至少选中1个形象
- 选中的形象中存在可上架的项目（状态为“待上架”或“已下架”）

#### 3.3.2.5.2 操作流程
1. 用户通过复选框选择需要上架的形象
2. 点击“批量上架”按钮
3. 系统校验：
   - 如未选中任何形象：提示“请先选择要上架的形象”
   - 如选中的形象中无可用项目：提示“所选形象中没有可上架的项目”
4. 系统批量处理：
   - 遍历所有选中的形象
   - 仅对状态为“待上架”或“已下架”的形象进行上架操作
   - 跳过已上架的形象
5. 操作完成后：
   - 显示成功提示：“成功上架 X 个形象”（X为实际上架数量）
   - 自动清空所有选择
   - 列表自动刷新

#### 3.3.2.5.3 业务规则
- **过滤规则**：仅对状态为“待上架”或“已下架”的形象进行上架
- **状态变更**：上架后形象状态变更为“已上架”
- **授权检测**：上架后系统开始每日检测该形象的授权时间

### 3.3.2.6 批量下架

#### 3.3.2.6.1 触发条件
- 至少选中1个形象
- 选中的形象中存在可下架的项目（状态为“已上架”）

#### 3.3.2.6.2 操作流程
1. 用户通过复选框选择需要下架的形象
2. 点击“批量下架”按钮
3. 系统校验：
   - 如未选中任何形象：提示“请先选择要下架的形象”
   - 如选中的形象中无可用项目：提示“所选形象中没有可下架的项目”
4. 系统批量处理：
   - 遍历所有选中的形象
   - 仅对状态为“已上架”的形象进行下架操作
   - 跳过非上架状态的形象
5. 操作完成后：
   - 显示成功提示：“成功下架 X 个形象”（X为实际下架数量）
   - 自动清空所有选择
   - 列表自动刷新

#### 3.3.2.6.3 业务规则
- **过滤规则**：仅对状态为“已上架”的形象进行下架
- **状态变更**：下架后形象状态变更为“已下架”
- **下架原因**：批量下架时，下架原因记录为“批量操作”

### 3.3.2.7 按钮状态
- **未选中时**：批量上架、批量下架按钮为禁用状态（disabled）
- **有选中时**：批量上架、批量下架按钮为可用状态

---

## 3.3.3 人物维度操作

### 3.3.3.1 操作入口
- **位置**：人物表格每行的“操作”列
- **操作按钮**：编辑、删除

### 3.3.3.2 编辑人物

#### 3.3.3.2.1 触发方式
- 点击人物行“操作”列中的“编辑”按钮

#### 3.3.3.2.2 编辑弹窗
- **弹窗标题**：编辑人物
- **弹窗宽度**：800px
- **表单字段**：与录入人物相同，但Face ID字段为禁用状态（不可修改）

#### 3.3.3.2.3 可编辑字段
| 字段名 | 是否可编辑 | 说明 |
|--------|----------|------|
| Face ID | 否 | 禁用状态，不可修改 |
| 主播名称（对内） | 是 | 可修改 |
| 对外人物名 | 是 | 可修改 |
| 类型 | 是 | 可修改 |
| 性别 | 是 | 可修改 |
| 年龄 | 是 | 可修改 |
| 头像 | 是 | 可重新上传 |
| 授权开始时间 | 是 | 可修改 |
| 授权结束时间 | 是 | 可修改 |

#### 3.3.3.2.4 提交逻辑
- 点击“确定”按钮提交修改
- 表单校验通过后，更新人物信息
- 显示成功提示：“人物信息更新成功”
- 列表自动刷新

### 3.3.3.3 删除人物

#### 3.3.3.3.1 触发方式
- 点击人物行“操作”列中的“删除”按钮

#### 3.3.3.3.2 删除确认
- **确认弹窗**：使用Popconfirm组件
- **确认文案**：“确定要删除这个人物吗？这将删除该人物下的所有形象。”
- **按钮**：确定、取消

#### 3.3.3.3.3 删除逻辑
- **级联删除**：删除人物时，同时删除该人物下的所有形象
- **软删除**：系统采用软删除机制（标记为已删除，不物理删除数据）
- **删除后处理**：
  - 显示成功提示：“人物删除成功”
  - 列表自动刷新，删除的人物不再显示

#### 3.3.3.3.4 业务规则
- **删除限制**：无特殊限制，可删除任何人物
- **数据影响**：删除人物会同时删除其下所有形象，操作不可逆

---

## 3.3.4 形象维度操作

### 3.3.4.1 操作入口
- **位置**：形象表格展开区域内
- **操作类型**：添加形象、编辑形象、删除形象、上架/下架形象

### 3.3.4.2 添加形象

#### 3.3.4.2.1 入口位置
- **位置**：形象表格上方，展开区域内
- **按钮样式**：默认按钮（Default），尺寸 Small
- **按钮文字**：添加形象
- **按钮图标**：+（PlusOutlined）

#### 3.3.4.2.2 空状态处理
- **有形象时**：显示“添加形象”按钮和形象表格
- **无形象时**：显示“添加形象”按钮和提示文字“暂无形象，点击上方按钮添加”

#### 3.3.4.2.3 添加弹窗
- **弹窗标题**：新增形象 - [对外人物名]
- **弹窗宽度**：800px
- **必填字段**：
  | 字段名 | 字段说明 | 校验规则 |
  |--------|---------|---------|
  | 形象Key | 形象唯一标识 | 必填；每个形象唯一 |
  | 特征 | 形象特征描述 | 必填；用于生成对外形象名称 |
  | 版式 | 横版/竖版 | 必填；默认横版 |

#### 3.3.4.2.4 选填字段
- 缩略图、风格（多选）、赛道、职业、姿势、比例、季节、场景、支持上传背景、宽、高

#### 3.3.4.2.5 业务规则
- **默认状态**：新添加的形象默认状态为“待上架”
- **命名规则**：对外形象名称自动生成，格式为“对外人物名-特征”
- **提交后处理**：
  - 显示成功提示：“形象添加成功”
  - 列表自动刷新
  - 如人物未展开，自动展开显示新添加的形象

### 3.3.4.3 编辑形象

#### 3.3.4.3.1 触发方式
- 点击形象行“操作”列中的设置图标（SettingOutlined）

#### 3.3.4.3.2 编辑弹窗
- **弹窗标题**：编辑形象 - [对外形象名称]
- **弹窗宽度**：800px
- **表单字段**：与添加形象相同，但形象Key为禁用状态（不可修改）

#### 3.3.4.3.3 特殊功能
- **上架/下架快捷操作**：弹窗底部提供“上架”/“下架”按钮，可直接修改上架状态
- **下架原因显示**：如形象已下架，显示下架原因

#### 3.3.4.3.4 提交逻辑
- 点击“确定”按钮提交修改
- 表单校验通过后，更新形象信息
- 显示成功提示：“形象信息更新成功”
- 列表自动刷新

### 3.3.4.4 删除形象

#### 3.3.4.4.1 触发方式
- 点击形象行“操作”列中的关闭图标（CloseOutlined）

#### 3.3.4.4.2 删除确认
- **确认弹窗**：使用Popconfirm组件
- **确认文案**：“确定要删除这个形象吗？”
- **按钮**：确定、取消

#### 3.3.4.4.3 删除限制
- **限制规则**：已上架的形象不允许删除
- **校验逻辑**：
  - 点击删除时，系统检查形象的上架状态
  - 如状态为“已上架”，显示警告提示：“已上架的形象不允许删除，请先下架”
  - 如状态为“待上架”或“已下架”，允许删除

#### 3.3.4.4.4 删除逻辑
- **软删除**：系统采用软删除机制
- **删除后处理**：
  - 显示成功提示：“形象删除成功”
  - 列表自动刷新
  - 如该人物下无其他形象，形象表格区域显示空状态

### 3.3.4.5 上架/下架形象

#### 3.3.4.5.1 操作位置
- **位置**：形象表格“上架状态”列
- **显示方式**：状态Tag + 操作按钮

#### 3.3.4.5.2 状态显示
- **已上架**：绿色Tag显示“已上架”，右侧显示“下架”按钮
- **待上架**：橙色Tag显示“待上架”，右侧显示“上架”按钮
- **已下架**：灰色Tag显示“已下架”，右侧显示“上架”按钮

#### 3.3.4.5.3 下架原因提示
- **显示方式**：鼠标悬停在状态Tag上时，Tooltip显示下架原因
- **显示内容**：下架原因：[原因内容]
- **显示条件**：仅当形象已下架且存在下架原因时显示

#### 3.3.4.5.4 上架操作
- **触发方式**：点击“上架”按钮
- **操作逻辑**：
  - 将形象状态从“待上架”或“已下架”变更为“已上架”
  - 清除下架原因（如有）
  - 显示成功提示：“形象已上架”
  - 系统开始每日检测该形象的授权时间

#### 3.3.4.5.5 下架操作
- **触发方式**：点击“下架”按钮
- **操作逻辑**：
  - 将形象状态从“已上架”变更为“已下架”
  - 记录下架原因：人工下架时记录为“人工下架”，系统自动下架时记录为“授权过期”
  - 显示成功提示：“形象已下架”

#### 3.3.4.5.6 授权过期自动下架
- **检测机制**：系统每日自动检测所有已上架形象的授权时间
- **下架条件**：当授权结束时间已过期时
- **下架处理**：
  - 自动将形象状态变更为“已下架”
  - 记录下架原因为“授权过期”
  - 无需人工干预

### 3.3.4.6 操作图标区分
- **编辑图标**：设置图标（SettingOutlined），与人物维度的编辑图标（EditOutlined）区分
- **删除图标**：关闭图标（CloseOutlined），与人物维度的删除图标（DeleteOutlined）区分
- **视觉区分**：形象维度的操作仅显示图标，不显示文字，通过title属性提供悬停提示

---

## 3.3.5 操作反馈

### 3.3.5.1 成功提示
- **提示方式**：使用Ant Design的message.success组件
- **显示位置**：页面右上角
- **显示时长**：3秒后自动消失
- **提示文案**：
  - 录入人物成功：“人物添加成功”
  - 编辑人物成功：“人物信息更新成功”
  - 删除人物成功：“人物删除成功”
  - 添加形象成功：“形象添加成功”
  - 编辑形象成功：“形象信息更新成功”
  - 删除形象成功：“形象删除成功”
  - 上架/下架成功：“形象已上架”/“形象已下架”
  - 批量操作成功：“成功上架 X 个形象”/“成功下架 X 个形象”

### 3.3.5.2 警告提示
- **提示方式**：使用Ant Design的message.warning组件
- **显示位置**：页面右上角
- **显示时长**：3秒后自动消失
- **提示场景**：
  - 删除已上架形象时：“已上架的形象不允许删除，请先下架”
  - 批量操作无可用项目时：“所选形象中没有可上架的项目”/“所选形象中没有可下架的项目”
  - 未选择形象时：“请先选择要上架的形象”/“请先选择要下架的形象”

### 3.3.5.3 错误提示
- **提示方式**：表单字段下方显示错误信息
- **显示规则**：表单校验失败时，在对应字段下方显示错误提示
- **常见错误**：
  - Face ID重复：“该Face ID已存在”
  - 必填字段为空：“请输入XXX”

---

## 3.3.6 数据同步

### 3.3.6.1 列表刷新
- **触发时机**：所有增删改操作成功后
- **刷新方式**：自动刷新当前列表数据
- **刷新范围**：仅刷新当前Tab（公有/私有）的数据

### 3.3.6.2 状态同步
- **选择状态**：批量操作完成后自动清空选择状态
- **展开状态**：添加形象后，如人物未展开，自动展开显示新形象
- **筛选状态**：操作完成后保持当前筛选条件

---

## 3.3.7 权限控制

### 3.3.7.1 操作权限
- **录入人物**：所有用户可操作
- **编辑人物**：所有用户可操作
- **删除人物**：所有用户可操作
- **添加形象**：所有用户可操作
- **编辑形象**：所有用户可操作
- **删除形象**：所有用户可操作（需满足删除限制）
- **上架/下架**：所有用户可操作
- **批量操作**：所有用户可操作

### 3.3.7.2 数据权限
- **公有数字人**：所有用户可见、可操作
- **私有数字人**：仅定制者可见、可操作（需根据实际业务需求调整）

---

## 3.3.8 性能优化

### 3.3.8.1 批量操作优化
- **批量处理**：批量操作时，系统一次性处理所有选中项，减少请求次数
- **状态更新**：批量操作后，批量更新状态，避免逐个更新

### 3.3.8.2 列表渲染优化
- **虚拟滚动**：当列表数据量大时，考虑使用虚拟滚动优化性能
- **懒加载**：形象表格采用展开时加载，减少初始渲染负担

---

## 3.3.9 异常处理

### 3.3.9.1 网络异常
- **处理方式**：操作失败时显示错误提示，保留用户输入数据
- **重试机制**：提供重试按钮，允许用户重新提交

### 3.3.9.2 数据异常
- **数据校验**：提交前进行完整的数据校验
- **错误提示**：校验失败时，在对应字段显示具体错误信息

### 3.3.9.3 并发冲突
- **乐观锁**：编辑操作时，如数据已被其他用户修改，提示用户刷新后重试
- **冲突提示**：检测到数据冲突时，显示明确的错误提示


